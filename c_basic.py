# List of subjects
subjects = ["Network", "Bio", "Programming", "Physics", "Music"]

# Iterate over each item in the list
for subject in subjects:
    # Check if the current item starts with the letter 'B'
    if subject.startswith("B"):
        # Print the item if it starts with 'B'
        print(subject)
